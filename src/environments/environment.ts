// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyB9n880816sxiGl_gbSpq_XOEdrkL4rqZM",
    authDomain: "showbuddy-ipm.firebaseapp.com",
    databaseURL: "https://showbuddy-ipm.firebaseio.com",
    projectId: "showbuddy-ipm",
    storageBucket: "showbuddy-ipm.appspot.com",
    messagingSenderId: "1015976937890",
    appId: "1:1015976937890:web:dfa25e7e1f1c00e7644afb"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
